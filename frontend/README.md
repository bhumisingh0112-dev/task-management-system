# Frontend

Create `.env.local` from `.env.local.example` if needed.

```bash
npm install
npm run dev
```

The task board uses the NestJS API at `NEXT_PUBLIC_API_URL` (default `http://localhost:4000`).
